#ifndef MEMORYALLOC_H#define MEMORYALLOC_H#include <stdio.h>#include <DriverServices.h>#include <DriverSupport.h>#include <MacTypes.h>#define SET_BIT(WORD, BIT) ( WORD |= (1 << BIT) )#define CLEAR_BIT(WORD, BIT) ( WORD &= ~(1 << BIT) )typedef struct dma_buffer_t{	LogicalAddress logical;	PhysicalAddress physical;	UInt32 length;} MEM_DMABuffer;void* MEM_AllocAlignedBlock(Size size, Size alignment);#define ALLOC_ALIGNED_BLOCK(T, S) ( ( T * ) MEM_AllocAlignedBlock(sizeof( T ), S ) )#define ALLOC_OBJECT_POOL(T, S, C) ( ( T * ) MEM_AllocAlignedBlock(sizeof( T ) * C , S ) )typedef struct ohci_device_t OHCI_Device;void MEM_PrepareForDMA(OHCI_Device *device);#endif