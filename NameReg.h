#ifndef NAMEREG_H#define NAMEREG_H#include <assert.h>#include <string.h>#include <stdio.h>#include <stdlib.h>#include <Interrupts.h>#include <MacTypes.h>#include <NameRegistry.h>typedef struct reg_property_t{	RegPropertyNameBuf 		name;		// 32-byte C-string	union	{		char 				valueStr32[32];		UInt32 				valueUint32;		ISTProperty			valueISTProperty;	};	RegPropertyValueSize 	valueSize;	// in bytes} KDRV_RegProperty;void KDRV_InitRegProperty(KDRV_RegProperty *prop, char *in_name, int in_size);OSErr KDRV_RegPropertyGet(RegEntryID *regEntry, KDRV_RegProperty *prop);OSErr KDRV_FindPCICardByName(RegEntryID *foundEntry, char *searchname);#endif